language: node_js

node_js:
  - "8"

cache:
  directories:
  - node_modules

before_install:
  - echo $GitHubPrivateKey >> test.txt
  - cat test.txt

install:
  - npm i

script:
- echo $GitHubPrivateKey >> test.t
- cat test.t
- npm run safe-sync